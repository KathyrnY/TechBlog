<main class="main">
  <div class="card-grid">
    {{#each posts}}
    <a href="homepage/comments/{{id}}" class="post-link">
<div class="card">
  <div class="card-body">
    <div>
        <h5 class="card-title">{{title}}</h5>
           
         <p>Created by {{user.username}} on {{formatDate date_created}}</p>
  </div>
  <div>
        <p class="card-text">{{content}}</p>
        
  </div>
  </div>
</div>
</a>
  {{/each}}
  </div>
    </main>